# main.tex v1.1 (2026-08-18) — single-thesis revision
Prior text: git HEAD (v1.0, June 2026). Build: `latexmk -pdf main.tex` → 6 pages (was 10),
3 figures + 2 tables, 0 overfull boxes.

Claim carried throughout: the apparent complexity gradient in position-averaged routing
entropy is produced by token position (per-token entropy rises with prefill position;
longer prompts pool more high-entropy positions; the graded levels are longer); the
final-token readout removes the gradient and reverses the extremes. Implication:
between-prompt routing-entropy comparisons, including workload/difficulty readings,
require a position-controlled readout or an explicit length/position model.

Changed: title; two-column 10pt layout; "Why routing entropy is tempting" folded into the
intro; results reorganized (4.1 gradient + length; 4.2 position diagnostic, now
quantitative, Fig 3 replaces the Spearman bar chart whose values Table 1 carries;
4.3 position-controlled readout + reversal; 4.4 replication/scope); Discussion carries the
implication with citations; the seven "tests that would weaken this reading" and the
"length-matched suite would settle" sentences removed; audit narrative reduced to one
provenance sentence in Data Availability; rank-sum statistic labeled z (scipy ranksums
standardized statistic; source table calls it W); em-dashes removed; UD-Q2_K_XL,
n_predict=0, prefill, final-token readout, position diagnostic defined.
Unchanged: every v1.0 value; refs.bib; the archive.
For a re-deposit: bump CITATION.cff / .zenodo.json title+version (not done here — Jeffrey's call).

## Structural alignment pass (2026-08-18, later; structure only, no science changed)
Reviewed against the abstract → intro → related work → method → setup → results (by
finding) → validity → discussion → limitations → conclusion → reproducibility template.
Verdict: architecture already matched; three moves applied (backup:
`main.PRE_structural_20260818.tex.bak`, build log `build_v11_structural.log`):
1. The transfer-across-models sentence ("Nothing in the mechanism is specific to these
   models: causal attention gives later positions more context in every decoder-only
   transformer, and the pattern appeared in every model run here.") moved from the
   Conclusion into the Discussion "Implication" paragraph, after the "counterpart in the
   router" sentence; the Conclusion keeps a one-clause echo ("Because the mechanism is not
   specific to these models, position or length should be treated as a default
   covariate…"). Conclusion now introduces no argument not made in the Discussion.
2. `\paragraph{Position diagnostic.}` split out in Methods (the one sentence that
   defined the five-prompt Qwen3.5-397B diagnostic; previously the tail of "Routing
   entropy and readouts").
3. Figure 1 and Figure 2 float anchors moved from the Methods source block to the top of
   Results (they are first cited in 4.1); compiled placement unchanged.
Kept as-is: Background name/placement, single Methods section with paragraph heads,
Results 4.1–4.4 (replication and scope checks stay in main text), Discussion →
Limitations → Conclusion, no appendix, no ethics section (n/a).
Build: `latexmk -pdf main.tex` → 6 pages, 0 overfull, values unchanged. PDF re-copied to
`job-search/paper-samples-pdf/02_Sequence_Position_Routing_Entropy_v1.1.pdf`.


## Addendum 2026-08-24
PDF rebuilt (no text change; a series note was added and then removed the same evening; decimal-number diff against the 2026-08-18 build is identical). Version string unchanged (1.1). Zenodo re-deposit still pending.
